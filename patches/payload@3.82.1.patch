diff --git a/dist/utilities/mergeLocalizedData.js b/dist/utilities/mergeLocalizedData.js
index 5c785c2a0b144daacacfca425158302f34759c16..3aaa7ffbb080b691670b399aa747de75bc369c6e 100644
--- a/dist/utilities/mergeLocalizedData.js
+++ b/dist/utilities/mergeLocalizedData.js
@@ -47,7 +47,7 @@ import { fieldAffectsData, fieldShouldBeLocalized, tabHasName } from '../fields/
                             } else if (Array.isArray(newValue)) {
                                 // Non-localized array - still process children for any localized fields
                                 result[field.name] = newValue.map((newItem, index)=>{
-                                    const existingItem = existingValue?.[index] || {};
+                                    const existingItem = (existingValue?.find((item) => item.id && item.id === newItem.id) || existingValue?.[index]) || {};
                                     return mergeLocalizedData({
                                         configBlockReferences,
                                         dataWithLocales: newItem,
@@ -98,7 +98,7 @@ import { fieldAffectsData, fieldShouldBeLocalized, tabHasName } from '../fields/
                                         block = field.blocks.find((b)=>b.slug === newBlockData.blockType);
                                     }
                                     if (block) {
-                                        const blockData = Array.isArray(existingValue) && existingValue[index] ? existingValue[index] : {};
+                                        const blockData = Array.isArray(existingValue) ? (existingValue.find((b) => b.id && b.id === newBlockData.id) || existingValue[index] || {}) : {};
                                         return mergeLocalizedData({
                                             configBlockReferences,
                                             dataWithLocales: newBlockData,
